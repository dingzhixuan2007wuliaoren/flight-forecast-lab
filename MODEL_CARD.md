# Model Card — Flight Forecast Lab

## 模型概览

Flight Forecast Lab 在同一工程中训练并部署两个独立的监督学习任务：

1. **票价回归**：预测指定未来行程的美元价格，并返回目标覆盖率为 80% 的预测区间。
2. **准点分类**：预测航班准点概率；正类定义为 `cancelled == false` 且 `arrival_delay_minutes < 15`。

仓库自带的默认产物由合成数据训练，只用于演示。除非模型信息明确记录了真实数据集版本、时间范围与评估结果，否则不得将其描述为真实航班预测模型。

## 基本信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | Flight Forecast Lab |
| Python 包 | `flight_forecaster` |
| 模型类型 | 直方图梯度提升回归与正则化逻辑回归流水线 |
| 接口 | CLI 与 FastAPI |
| 许可证 | MIT（外部数据不包含在许可证授权范围内） |
| 默认模型状态 | 合成数据演示，不适用于生产 |

每次正式发布模型时，应在模型目录或发布说明中补充：代码提交号、训练时间、数据快照标识、特征模式版本、随机种子、依赖锁定信息和负责人。

## 预期用途

适合：

- 教学、作品集和机器学习工程流程演示；
- 比较航线、日期或行程属性变化下的相对预测；
- 在完成真实数据训练与独立验证后，为人工决策提供辅助信号；
- 验证批处理、API 服务、模型监控和数据契约。

不适合：

- 保证某张机票未来一定涨价或降价；
- 替代航空公司、机场或监管机构发布的实时运行状态；
- 自动交易、差别定价、退款资格、信用、保险或其他高影响决策；
- 安全关键的调度或运行控制；
- 将美国公开数据训练出的模型无验证地外推到其他国家、机场或销售渠道。

## 输入与输出

### 票价任务

主要输入为起点、终点、航司、舱位、经停数、行程时长、距离、询价时间与起飞时间。输出字段为 `estimated_price_usd`、`interval_80_low_usd`、`interval_80_high_usd`、提前天数、模型版本和用途警告。时间字段必须带明确时区；货币统一为美元，距离统一为公里。

区间含义是**单次未来观测的预测不确定性**，不是均值置信区间，也不是价格上下限保证。80% 是目标经验覆盖率，必须在独立的、时间上更晚的校准/测试数据上复核。

### 准点任务

主要输入为起点、终点、航司、距离、计划起飞时间、预测天气严重度与起飞机场拥堵指数。输出为准点概率、扰动概率以及由准点概率生成的 `low` / `medium` / `high` 扰动风险等级。当前服务在准点概率不低于 0.80 时标为低风险，0.60–0.80 时标为中风险，低于 0.60 时标为高风险。

正类标签：

```text
on_time = (not cancelled) AND (arrival_delay_minutes < 15)
```

因此，取消航班始终为负类；到达延误恰好 15 分钟也为负类。改签、备降和缺少最终到达记录的样本必须按预先声明的规则处理，不能静默当作准点。

完整字段约束见 [`docs/data-contracts.md`](docs/data-contracts.md)。

## 训练数据

### 演示模式

`train-demo` 使用程序生成的合成样本，并注入可学习的季节、航线、航司、天气和拥堵关系。它的目的只是让训练与服务在没有外部下载时可重复运行。合成分布、类别比例和噪声均为人为设定，得到的指标不能代表真实世界性能。

### 真实数据候选

- BTS DB1B/DB1C：已售客票抽样调查，可用于历史票价建模；
- BTS Reporting Carrier On-Time Performance：航班级计划和实际运行记录，可生成准点标签；
- NOAA/NCEI：机场附近历史天气观测，可用于回溯特征；部署时应改接在预测时刻可获得的天气预报特征。

数据来源、制度变更和连接风险见 [`docs/data-sources.md`](docs/data-sources.md)。

## 建议训练与评估方法

训练流程按时间排序，使用最早 70% 训练、中间 15% 校准、最新 15% 测试。票价模型对 `log1p(price_usd)` 拟合直方图梯度提升回归；80% 区间使用校准集绝对残差的有限样本 conformal 分位数，形成非负、对称的价格区间。准点任务使用带 L2 正则化的逻辑回归输出概率，并以 0.5 作为当前评估阈值。

这种单次时间切分适合作为清晰基线，不替代生产评估。真实模型还应进行多个向前滚动窗口的稳定性检查；不得随机打散所有年份后只报告单一分数。

建议报告：

| 任务 | 核心指标 | 必须附带的诊断 |
| --- | --- | --- |
| 票价 | 当前产物：MAE、RMSE、R²、中位数基线 MAE | 当前产物另含 80% 区间实际覆盖率和平均宽度；正式发布还应增加按航线/航司/月份/舱位切片 |
| 准点 | 当前产物：ROC-AUC、Brier score、log loss、0.5 阈值准确率 | 当前产物另含基准 Brier score；正式发布还应增加 PR-AUC、召回率、F1、校准曲线、混淆矩阵与关键切片 |

不要只选择对模型最有利的阈值。准点分类阈值应依据部署成本预先确定，并与概率校准质量分开报告。

本仓库不在模型卡中填入虚构指标。真实数值应从对应模型产物的训练摘要中读取，并与数据快照和代码版本一起发布。

## 不确定性与校准

- 票价区间是用校准残差生成的对称、全局 conformal 区间，只对与校准样本近似可交换的分布具有经验意义；需要按时间和关键切片检查覆盖率。
- 单一全局区间可能掩盖长尾航线、商务舱、节假日或临近起飞样本的不确定性。
- 准点概率需要用可靠性图和 Brier score 检查；高 AUC 不代表概率已经校准。
- 当输入类别未见、时间超出训练范围、数据缺失或明显越界时，服务应拒绝请求或发出可观察的告警，而不是伪造确定性。

## 已知限制与偏差来源

1. **抽样与覆盖偏差**：DB1B/DB1C 是报告承运人的客票样本；覆盖市场、渠道和承运人结构可能与目标用户不同。
2. **价格语义差异**：调查中的已支付票价不等于某一时刻网页上的可售报价，也未必包含相同的税费、附加服务和退款条件。
3. **发布时间滞后**：公开统计不能捕捉刚发生的竞争、成本或运力变化。
4. **标签偏差**：准点的二元阈值会把 14 分钟和 15 分钟延误分到不同类别；取消与严重延误被压缩为同一负类。
5. **泄漏风险**：实际到达后才知道的天气、拥堵、延误原因或后续航班信息不能作为未来推理特征。
6. **地理外推**：以美国 BTS 记录训练的模型不应默认适用于加拿大、欧洲或其他地区。
7. **异常事件**：罢工、极端天气、公共卫生事件、系统故障和监管变化会造成训练分布外预测。
8. **群体影响**：机场和航线与地区收入、可达性等社会因素相关。即使模型不输入敏感属性，商业使用仍可能产生不均衡影响。

## 隐私与安全

官方聚合/公开记录不应与可识别个人的搜索或购买日志混合发布。若接入企业数据，应最小化字段、限制保留期限、隔离原始标识，并进行适用法律和合同审查。

模型文件应来自受信任的构建流程。不要加载来源不明的序列化模型；部署时应记录文件哈希、限制目录权限，并对 API 实施请求大小限制、身份验证、速率限制和结构化日志。

## 上线前检查

- [ ] 训练数据不是合成数据，并有可追溯的快照和字段字典；
- [ ] 使用严格的时间切分，所有特征在预测时刻可获得；
- [ ] 在最新留出集报告了上述指标和关键切片；
- [ ] 票价区间的总体及切片覆盖率经过验证；
- [ ] 准点概率经过校准检查，分类阈值有业务依据；
- [ ] 未见类别、缺失值、时区和极端值行为经过测试；
- [ ] 已定义漂移、性能、覆盖率和校准监控及回滚条件；
- [ ] UI/API 明示预测时间、模型版本、数据新鲜度与限制；
- [ ] 完成人工审核、隐私审查和安全评估。

## 监控与再训练

至少监控输入缺失率、类别未见率、特征分布漂移、请求失败率和预测分布。待真实结果回流后，再监控票价 MAE、区间覆盖率、准点 Brier score、校准误差以及关键切片差异。

当数据制度、航司代码、机场映射或产品定义变化时，应触发契约审查而不是自动吞入。再训练模型必须保留上一版本作为可回滚基线。
